import { Button } from "@chakra-ui/react";

export const FeedbackButton = () => {
    return <Button
        bg="white"
        size="sm"
        fontWeight="normal"
        fontSize="sm"
        onClick={() => {
            return null;
        }}
        borderRadius="sm"
    >
        Give Feedback
    </Button>
}